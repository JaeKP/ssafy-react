{
  "scripts": {
    "start": "run-p watch dev",
    "dev": "live-server public --host=localhost --port=3000 --cors --no-browser",
    "compile": "babel src -d public/compiled",
    "watch": "npm run compile -- --watch",
    "build": "babel src -d public/build --minified"
  },
  "devDependencies": {
    "@babel/cli": "7.18.9",
    "@babel/core": "7.18.9",
    "@babel/preset-env": "7.18.9",
    "@babel/preset-react": "7.18.6",
    "eslint": "8.20.0",
    "eslint-plugin-react": "7.30.1",
    "live-server": "1.2.2",
    "npm-run-all": "4.1.5"
  }
}