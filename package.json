{
  "scripts": {
    "start": "live-server public --host=localhost --port=3000 --cors",
    "dev": "npm start -- --no-browser"
  },
  "devDependencies": {
    "eslint": "8.20.0",
    "eslint-plugin-react": "7.30.1",
    "live-server": "1.2.2"
  }
}